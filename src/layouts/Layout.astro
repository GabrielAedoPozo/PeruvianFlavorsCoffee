---
import Footer from "../components/footer.astro";
import Header from "../components/header.astro";
import '@fontsource/quicksand/300.css';
import '@fontsource/quicksand/400.css';
import '@fontsource/quicksand/500.css';
import '@fontsource/quicksand/600.css';
---

<!doctype html>
<html lang="es">
  <head>
    <!-- Configuración general -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/PeruvianFlavorsCoffee/logo.png" />
    <meta name="generator" content={Astro.generator} />

    <!-- SEO Básico -->
    <title>Peruvian Flavors Coffee</title>
    <meta
      name="description"
      content="Peruvian Flavors Coffee ofrece café peruano de especialidad, 100% arábica y cultivado en los Andes del Perú. Café de exportación, ético, sostenible y recién tostado."
    />
    <meta
      name="keywords"
      content="café peruano, café de especialidad del Perú, café arábica peruano, café andino, café peruano de exportación, mejor café del Perú, café sostenible, café ético, café de los Andes, café peruano tostado, café premium peruano"
    />
    <meta name="author" content="Peruvian Flavors Coffee" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="https://www.peruvianflavorscoffee.com/" />

    <!-- Open Graph / Facebook -->
    <meta property="og:title" content="Peruvian Flavors Coffee | Café Peruano De Especialidad" />
    <meta
      property="og:description"
      content="Descubre el auténtico sabor del Perú. Café arábica de los Andes, cultivado de forma sostenible y recién tostado."
    />
    <meta property="og:image" content="https://www.peruvianflavorscoffee.com/images/og-image.jpg" />
    <meta property="og:url" content="https://www.peruvianflavorscoffee.com/" />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="es_PE" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Peruvian Flavors Coffee | Café Peruano de Especialidad 100% Arábica" />
    <meta
      name="twitter:description"
      content="Café peruano premium, cultivado en los Andes y tostado artesanalmente. Sabor auténtico, aroma intenso."
    />
    <meta name="twitter:image" content="https://www.peruvianflavorscoffee.com/images/twitter-card.jpg" />

    <!-- SEO Local -->
    <meta name="geo.region" content="PE-LIM" />
    <meta name="geo.placename" content="Lima, Perú" />
    <meta name="geo.position" content="-12.0464;-77.0428" />
    <meta name="ICBM" content="-12.0464, -77.0428" />

    <!-- Schema.org JSON-LD (SEO Avanzado) -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "Peruvian Flavors Coffee",
      "url": "https://www.peruvianflavorscoffee.com",
      "logo": "https://www.peruvianflavorscoffee.com/PeruvianFlavorsCoffee/logo.png",
      "sameAs": [
        "https://www.instagram.com/peruvianflavorscoffee",
        "https://www.facebook.com/peruvianflavorscoffee",
        "https://www.linkedin.com/company/peruvianflavorscoffee"
      ],
      "description": "Peruvian Flavors Coffee es una marca peruana de café de especialidad. Ofrecemos café 100% arábica cultivado en los Andes del Perú, ético, sostenible y recién tostado.",
      "address": {
        "@type": "PostalAddress",
        "addressCountry": "PE",
        "addressLocality": "Lima",
        "addressRegion": "Lima",
        "postalCode": "15001"
      },
      "founder": "Peruvian Flavors Coffee",
      "foundingDate": "2020",
      "slogan": "El auténtico sabor del café peruano"
    }
    </script>

  <!-- Colores para dispositivos -->
  <meta name="theme-color" content="#3b2f2f" />

  <!-- Font Awesome -->
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
  />
  </head>

  <body>
    <div class="wrapper">
      <Header />

      <main>
		<slot />
      </main>

      <Footer />
    </div>

    <a href="https://wa.me/51987800910?text=Hola!%20estoy%20interesado%20en:" class="whatsapp-button" target="_blank" rel="noopener noreferrer">
      <i class="fab fa-whatsapp"></i>
    </a>

    <script>
      import '../scripts/app.js';
    </script>
  </body>

  <style is:global>
    :root {
      --max-width: 1200px;
      --padding-x: 2rem;
      --padding-y: 4rem;
      /* Paleta café consistente */
      --color-bg: #18100c;
      --color-bg-alt: #211510;
      --color-layer: #271a14;
      --color-layer-soft: #2f1e17;
      --color-surface: #3a241a;
      --grad-accent: linear-gradient(90deg, #ffffff 0%, #ffffff 60%, #f7f7f7 100%);
      --color-accent-alt: #a86b33;
      --color-accent-soft: #e2ba87;
      --color-accent-fade: rgba(197,138,75,0.18);
      --color-highlight: #f0c040; 
      --color-border: rgba(255,255,255,0.08);
      --color-border-strong: rgba(255,255,255,0.14);
      --color-text: #f5efe9;
      --color-text-soft: #ded4cc;
      --color-text-muted: #b79f91;
      --shadow-soft: 0 6px 18px rgba(0,0,0,0.28);
      --shadow-hover: 0 10px 26px rgba(0,0,0,0.34);
      --grad-accent: linear-gradient(90deg, #ffffff 0%, #ffffff93 60%, #f7f7f7 100%);
      --text-glow: 0 0 10px rgba(255,255,255,0.22), 0 0 22px rgba(255,255,255,0.12);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      scroll-behavior: smooth;
    }

    @media (max-width: 768px) {
      :root {
        --padding-x: 1.5rem;
        --padding-y: 3rem;
      }
    }

    @media (max-width: 480px) {
      :root {
        --padding-x: .8rem;
        --padding-y: 1.8rem;
      }
      
      body {
        font-size: 15px;
        line-height: 1.5;
      }
    }

    @media (max-width: 360px) {
      :root {
        --padding-x: .7rem;
      }
      
      .whatsapp-button {
        width: 45px;
        height: 45px;
        font-size: 1.5rem;
        bottom: 15px;
        right: 15px;
      }
    }

    body {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Quicksand', sans-serif;
      min-height: 100vh;
      background-color: var(--color-bg);
      color: var(--color-text);
      overflow-x: hidden;
      line-height: 1.45;
    }

    @media (max-width: 560px) {
      body {
        line-height: 1.5;
      }

      main {
        padding-bottom: 2rem;
      }
    }

    .wrapper {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      width: 100%;
      max-width: 100vw;
      background-color: #180f0f;
    }

    main {
      flex: 1;
      width: 100%;
      position: relative;
    }

    /* Global responsive images and media */
    img, svg, video, canvas { max-width: 100%; height: auto; }

    /* Prevent overflow from flex children on small screens */
    main { min-width: 0; }

    .whatsapp-button {
      position: fixed;
      bottom: 30px;
      right: 30px;
      background: #25d366;
      color: white;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2rem;
      box-shadow: 0 4px 10px rgba(0,0,0,0.3);
      transition: all 0.3s ease;
      z-index: 1000;
      animation: pulse 2s infinite;
    }

    a.whatsapp-button {
      text-decoration: none;
      color: inherit;
    }

    .whatsapp-button:hover {
      transform: translateY(-5px);
      background: #25d365da;
    }

    @keyframes pulse {
      0% {
        box-shadow: 0 0 0 0 rgba(37, 211, 102, 0.5);
      }
      70% {
        box-shadow: 0 0 0 15px rgba(37, 211, 102, 0);
      }
      100% {
        box-shadow: 0 0 0 0 rgba(37, 211, 102, 0);
      }
    }

    @media (max-width: 768px) {
      .whatsapp-button {
        width: 50px;
        height: 50px;
        font-size: 1.8rem;
        bottom: 20px;
        right: 20px;
      }
    }

    @media (max-width: 360px) {
      .whatsapp-button {
        width: 45px;
        height: 45px;
        font-size: 1.5rem;
        bottom: 15px;
        right: 15px;
      }
    }

    /* Preferencia reduce motion */
    @media (prefers-reduced-motion: reduce) {
      * {
        animation-duration: .01ms !important;
        animation-iteration-count: 1 !important;
        transition: none !important;
      }
    }

            font-size: 1.8rem;
        bottom: 50px; /* adjusted for mobile */
        right: 20px;
      }

      .price-disclaimer {
        bottom: 5px; /* close to bottom on mobile */
        right: 20px;
        max-width: 180px;
        padding: 0.4rem 0.6rem;
      }

      .price-disclaimer p {
        font-size: 0.65rem;
      }
    }

    @media (max-width: 480px) {
      .price-disclaimer {
        bottom: 3px; /* very close to bottom on small mobile */
        right: 15px;
        max-width: 160px;
        padding: 0.3rem 0.5rem;
      }

      .price-disclaimer p {
        font-size: 0.6rem;
      }
    }

    @media (max-width: 360px) {
      .whatsapp-button {
        width: 45px;
        height: 45px;
        font-size: 1.5rem;
        bottom: 45px; /* adjusted for very small screens */
        right: 15px;
      }

      .price-disclaimer {
        bottom: 2px; /* minimal space from bottom */
        right: 15px;
        max-width: 140px;
        padding: 0.25rem 0.4rem;
      }

      .price-disclaimer p {
        font-size: 0.55rem;
      }
    }

    /* Preferencia reduce motion */
    @media (prefers-reduced-motion: reduce) {
      * {
        animation-duration: .01ms !important;
        animation-iteration-count: 1 !important;
        transition: none !important;
      }
    }
  </style>
</html>
